# Everything the image doesn't need. Keeps the build context small and stops
# unrelated edits (docs, screenshots, git history) from busting the layer cache.
.git
.github
docs
tests
completions
*.png
*.md
Makefile
install.sh
claude-sandbox
